A five-minute walkthrough of the project: the problem, how the drainage basins and covariates are built, what the models do, and where they succeed and fail.

**The numbers quoted in the video are out of date.** It was recorded before additional tweaks and tuning rounds, so the scores it cites are lower than the current ones *and* were computed under a more forgiving split. The figures in *About this project* and *Results & metrics* are the ones to quote.

Everything else — the motivation, the data pipeline, and the reasoning behind the feature design — still describes the project as built.
